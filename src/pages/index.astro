---
---
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome to Dockit Astro</title>
    <style>
      body {
        font-family: system-ui, sans-serif;
        margin: 0;
        padding: 0;
        background: #f8fafc;
        min-height: 100vh;
      }
      .hero {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;
        padding: 3rem 1rem 2rem 1rem;
        background: #fff;
        box-shadow: 0 2px 8px rgba(0,0,0,0.04);
        max-width: 900px;
        margin: 2rem auto 2rem auto;
        border-radius: 1.5rem;
        gap: 2rem;
      }
      .hero-img {
        width: 320px;
        height: 220px;
        object-fit: cover;
        border-radius: 1rem;
        background: #e5e7eb;
        box-shadow: 0 2px 8px rgba(0,0,0,0.06);
      }
      .hero-content {
        flex: 1;
        min-width: 220px;
      }
      .hero-content h1 {
        font-size: 2.5rem;
        margin-bottom: 0.5em;
        color: #222;
      }
      .hero-content p {
        font-size: 1.25rem;
        margin-bottom: 1.5em;
        color: #555;
      }
      .form-section {
        background: #fff;
        max-width: 500px;
        margin: 2rem auto;
        padding: 2rem 2rem 1.5rem 2rem;
        border-radius: 1.5rem;
        box-shadow: 0 2px 8px rgba(0,0,0,0.04);
      }
      form {
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }
      label {
        font-weight: 500;
        margin-bottom: 0.25rem;
      }
      input, textarea {
        padding: 0.75em;
        border: 1px solid #d1d5db;
        border-radius: 0.5em;
        font-size: 1rem;
        background: #f9fafb;
        resize: vertical;
      }
      textarea {
        min-height: 80px;
        max-height: 200px;
      }
      button[type="submit"] {
        background: #6366f1;
        color: #fff;
        border: none;
        border-radius: 0.5em;
        padding: 0.75em 2em;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        margin-top: 0.5rem;
        transition: background 0.2s;
      }
      button[type="submit"]:hover {
        background: #4f46e5;
      }
    </style>
  </head>
  <body>
    <section class="hero">
      <img src="/hero.jpg" alt="Hero" class="hero-img" loading="lazy" />
      <div class="hero-content">
        <h1>Welcome to Dockit Astro!</h1>
        <p>Your Astro-powered documentation site is ready. Start building your next project with speed and flexibility.</p>
      </div>
    </section>
    <section class="form-section">
      <form name="contact" method="POST" netlify>
        <div>
          <label for="firstName">First Name</label>
          <input type="text" id="firstName" name="firstName" required />
        </div>
        <div>
          <label for="lastName">Last Name</label>
          <input type="text" id="lastName" name="lastName" required />
        </div>
        <div>
          <label for="company">Company Name</label>
          <input type="text" id="company" name="company" />
        </div>
        <div>
          <label for="phone">Phone Number</label>
          <input type="tel" id="phone" name="phone" />
        </div>
        <div>
          <label for="email">Email</label>
          <input type="email" id="email" name="email" required />
        </div>
        <div>
          <label for="message">Message</label>
          <textarea id="message" name="message" required></textarea>
        </div>
        <button type="submit">Submit</button>
      </form>
    </section>
  </body>
</html>
